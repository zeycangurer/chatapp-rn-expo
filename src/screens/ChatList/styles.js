import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    addFAB: {
        position: 'absolute',
        bottom: 16,
        right: 16
    }
})

export default styles